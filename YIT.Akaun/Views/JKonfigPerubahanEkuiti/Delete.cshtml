@model JKonfigPerubahanEkuiti

@{
    ViewData["Title"] = "Konfigurasi Perubahan Ekuiti - Hapus";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="card box-shadow">
    <div class="card-body">
        <partial name="~/Views/Partials/Common/_TitleDeletePartial.cshtml" />

        <p class="text-danger">Anda pasti untuk menghapuskan maklumat berikut?</p>
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.EnLajurJadual)
            </dt>
            <dd class="col-sm-10">
                @Model.EnLajurJadual.GetDisplayName()
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.JKWId)
            </dt>
            <dd class="col-sm-10">
                @BelanjawanFormatter.ConvertToKW(Model.JKW?.Kod) - @Model.JKW?.Perihal
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Tahun)
            </dt>
            <dd class="col-sm-10">
                @Model.Tahun
            </dd>
            @*<dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.KodBakiAwalList)
            </dt>
            <dd class="col-sm-10">
                Kod Terlibat :
                @foreach (var s in ViewBag.SelectedKodBakiAwalList)
                {
                    <span class="badge ac-secondary">@s</span>
                }
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.KodPelarasanList)
            </dt>
            <dd class="col-sm-10">
                Kod Terlibat :
                @foreach (var s in ViewBag.SelectedKodPelarasanList)
                {
                    <span class="badge ac-secondary">@s</span>
                }
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.KodLebihanList)
            </dt>
            <dd class="col-sm-10">
                Kod Terlibat :
                @foreach (var s in ViewBag.SelectedKodLebihanList)
                {
                    <span class="badge ac-secondary">@s</span>
                }
            </dd>*@

        </dl>
        <hr />
        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <input name="syscode" value="SPPB" hidden />
            <input type="submit" value="Hapus" class="btn ac-danger btn-sm" />
            <a asp-action="Index" class="btn ac-secondary btn-sm" style="width:auto">Batal</a>
        </form>
    </div>
</div>
